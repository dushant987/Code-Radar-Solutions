#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    int a ,i,sum;
    scanf("%d",&a);
    for (i=1;i<=n;i++);
    {
        if(a%i==0)
        sum=sum+1;
    }
    if (sum==2)
    printf("Prime");
    else
    printf("Not Prime");
    return 0;
}